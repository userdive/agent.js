box: node
build:
  steps:
    - install-packages:
        packages: ocaml libelf-dev
    - script:
        name: logger
        code: |
          cd @userdive/logger && npm install
          npm test
