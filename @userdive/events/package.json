{
  "name": "@userdive/events",
  "description": "bind browser events and dispatch tracking data",
  "version": "0.0.1",
  "author": "UNCOVER TRUTH Inc.",
  "dependencies": {
    "mitt": "^1.0.1",
    "throttle-debounce": "^1.0.1",
    "ui-event-observer": "^1.1.0"
  },
  "devDependencies": {
    "@uncovertruth/eslint-config-flowtype": "0.0.2",
    "@userdive/logger": "file:../logger",
    "assert-exception": "^1.1.0",
    "babel-cli": "^6.22.1",
    "babel-plugin-transform-es2015-modules-commonjs": "^6.22.0",
    "babel-plugin-transform-flow-strip-types": "^6.22.0",
    "babel-preset-power-assert": "^1.0.0",
    "babel-register": "^6.22.0",
    "eslint": "^3.14.0",
    "faker": "^3.1.0",
    "flow-bin": "^0.38.0",
    "jsdom": "^9.9.1",
    "jsdom-global": "^2.1.1",
    "jsdom-mount": "^0.1.0",
    "mocha": "^3.2.0",
    "npm-run-all": "^4.0.1",
    "nyc": "^10.1.2",
    "power-assert": "^1.4.2",
    "raven-js": "^3.9.2",
    "rimraf": "^2.5.4",
    "sinon": "^1.17.7"
  },
  "directories": {
    "test": "test"
  },
  "keywords": [
    "userdive"
  ],
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm-run-all -p build:*",
    "build:babel": "mkdir -p dist && babel src -d dist",
    "clean": "rimraf coverage dist",
    "lint": "npm-run-all -p lint:*",
    "lint:es": "eslint . --cache",
    "lint:flow": "flow",
    "module": "npm-run-all -p module:*",
    "module:logger": "babel node_modules/@userdive/logger/src -d node_modules/@userdive/logger/dist",
    "prebuild": "npm run clean",
    "pretest": "npm-run-all -p clean module",
    "test": "npm-run-all test:*",
    "test:lint": "npm run lint",
    "test:mocha": "nyc mocha -- test"
  }
}
