{
  "name": "userdive-examples",
  "version": "1.1.0",
  "devDependencies": {
    "gh-pages": "1.2.0",
    "pug": "2.0.3",
    "pug-cli": "1.0.0-alpha6",
    "testcafe": "^0.20.2"
  },
  "license": "MIT",
  "main": "n/a",
  "private": true,
  "scripts": {
    "build": "run-p build:*",
    "build:html": "pug . -o build/agent.js -P -s",
    "build:js": "webpack --env production",
    "clean": "rimraf tests build",
    "e2e": "testcafe 'chrome:headless --no-sandobox' test/",
    "prebuild": "npm run clean"
  }
}
