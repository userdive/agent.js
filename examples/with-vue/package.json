{
  "name": "with-vue",
  "version": "0.14.0-beta1",
  "dependencies": {
    "userdive": "^0.14.0-beta1",
    "vue": "^2.5.13",
    "vue-router": "^3.0.1"
  },
  "private": true,
  "devDependencies": {
    "chromedriver": "^2.35.0",
    "nightwatch": "^0.9.20",
    "selenium-server-standalone-jar": "^3.9.1"
  },
  "scripts": {
    "build": "tsc ./*.test.ts --lib es6 --outDir tests",
    "test": "nightwatch",
    "prebuild": "rimraf ./*.test.js",
    "pretest": "npm run build"
  }
}
