{
  "author": "UNCOVER TRUTH Inc.",
  "bugs": {
    "url": "https://github.com/userdive/agent.js/issues"
  },
  "devDependencies": {
    "@uncovertruth/eslint-config": "^3.4.0",
    "@uncovertruth/tslint-config": "^1.3.0",
    "codecov": "^3.0.0",
    "cross-env": "^5.1.3",
    "eslint": "^4.17.0",
    "husky": "^0.14.3",
    "lerna": "^2.9.0",
    "lint-staged": "^6.1.0",
    "npm-run-all": "^4.1.2",
    "prettier-eslint-cli": "^4.7.1",
    "rimraf": "^2.6.2",
    "size-limit": "^0.14.1",
    "string-replace-loader": "^1.3.0",
    "ts-loader": "^3.5.0",
    "tslint": "^5.9.1",
    "typescript": "^2.7.2",
    "uglify-js": "^3.3.11",
    "uglifyjs-webpack-plugin": "^1.1.8",
    "webpack": "^3.11.0"
  },
  "engine": {
    "node": "> 5"
  },
  "homepage": "https://github.com/userdive/agent.js#readme",
  "keywords": [
    "userdive"
  ],
  "license": "GPL-3.0",
  "lint-staged": {
    "*.ts": [
      "prettier-eslint --write",
      "tslint --fix",
      "git add"
    ],
    "*.md": [
      "prettier --write",
      "git add"
    ]
  },
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/userdive/agent.js.git"
  },
  "scripts": {
    "build": "run-p build:*",
    "build:cdn": "webpack --env=production && size-limit",
    "build:packages": "lerna run build",
    "bump": "lerna publish --skip-npm --skip-git",
    "ci:sauce": "cross-env CI_MODE=sauce lerna run test",
    "clean": "lerna clean --yes && lerna run clean && rimraf cdn",
    "coverage": "run-p coverage:*",
    "coverage:agent": "codecov -f ./packages/agent/coverage/lcov.info -F agent",
    "lint": "run-p lint:*",
    "lint:ts": "tslint 'packages/*/{src,test}/**/*.ts'",
    "postbuild": "cp robots.txt ./cdn",
    "precommit": "lint-staged",
    "prepare": "lerna run bootstrap",
    "publish": "lerna publish --skip-git",
    "test": "lerna run test"
  },
  "size-limit": [
    {
      "path": "cdn/agent.js",
      "limit": "8 KB"
    }
  ],
  "workspaces": [
    "packages/*"
  ]
}
